# دليل رفع وعرض الصور في Dashboard

## ✅ التحديثات المنجزة

تم تحديث لوحة تحكم الأدمن بشكل كامل لدعم رفع وعرض الصور.

### الملفات المحدثة:
- `frontend/html/admin-dashboard.html` - تم تحديثه بالكامل

### المميزات المضافة:

#### 1. حقول رفع الملفات
تم إضافة حقول `input type="file"` في جميع النماذج:
- Main Category Modal
- Category Modal  
- Restaurant Modal

كل نموذج يحتوي على:
- حقل رفع صورة جديد
- حقل رابط الصورة (للاحتياطي)
- رسالة توضيحية (5MB max, صيغ مدعومة)

#### 2. دالة API جديدة
تم إضافة دالة `apiCallFormData` لرفع الملفات:
```javascript
async function apiCallFormData(endpoint, method = 'POST', formData)
```

#### 3. تحديث دوال الحفظ
تم تحديث دوال:
- `saveMainCategory` - دعم FormData عند رفع ملف
- `saveCategory` - دعم FormData عند رفع ملف
- `saveRestaurant` - دعم FormData عند رفع ملف

كل دالة تحتوي على منطق:
- إذا كان هناك ملف: استخدام FormData وإرسال multipart/form-data
- إذا كان هناك رابط فقط: استخدام JSON عادي

#### 4. عرض الصور
تم تحديث دوال العرض:
- `renderMainCategories` - عرض الصور في كروت الأقسام الرئيسية
- `renderCategories` - عرض الصور في كروت الأقسام
- `renderRestaurants` - عرض الصور في كروت المطاعم

كل بطاقة تعرض:
- صورة 160px ارتفاع
- container مستدير بخلفية رمادية فاتحة
- object-cover للمحافظة على النسبة

## 🎯 كيفية الاستخدام

### رفع صورة جديدة:
1. افتح نموذج الإنشاء/التعديل
2. اختر "رفع صورة" أو أدخل "رابط الصورة"
3. اضغط "حفظ"
4. ستظهر الصورة تلقائياً في القائمة

### عرض الصور:
- الصور تظهر في أعلى كل بطاقة
- إذا لم تكن هناك صورة، لن يظهر مكان الصورة (تصميم نظيف)
- الصور responsive وتتكيف مع حجم الشاشة

## 🔗 التكامل مع Backend

النظام مرتبط تماماً مع:
- `/api/AdminDashboard/main-categories` - POST, PUT
- `/api/AdminDashboard/categories` - POST, PUT
- `/api/AdminDashboard/restaurants` - POST, PUT

الـ Backend يدعم:
- رفع الملفات multipart/form-data
- استخدام URL خارجي
- التحقق من صحة الصور
- حذف الصور القديمة تلقائياً

## 🎨 التصميم

الصور تعرض بشكل احترافي:
- Height ثابت: 160px
- Object-fit: cover
- Border radius: أعلى فقط
- Hover effects على البطاقات
- Responsive على جميع الأجهزة

## 🚀 جاهز للاستخدام

النظام جاهز بالكامل! فقط:
1. ابدأ الـ Backend: `dotnet run`
2. افتح Dashboard: `admin-dashboard.html`
3. ارفع الصور واستمتع! 🎉

